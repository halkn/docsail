# v0.1.0 Issue 計画

Milestone は `v0.1.0 — Workspace Viewer` とする。以下は GitHub Issue 作成前の文書案であり、現時点では Issue・ラベル・Milestone を GitHub 上へ作成しない。

`priority:p0` は #1 のみとし、他は原則 `priority:p1` とする。基盤に関する Issue は `area:infra`、領域ごとに `area:workspace`、`area:tui`、`area:markdown`、Terminal 固有のものは `area:terminal` を付与する。

| # | 件名 | ラベル | 主な依存 |
| --- | --- | --- | --- |
| 1 | Rust プロジェクトと基本 CLI を初期化する | `type:feature`, `area:infra`, `priority:p0` | なし |
| 2 | Ratatui の Terminal 初期化・復元処理を実装する | `type:feature`, `area:terminal`, `priority:p1` | #1 |
| 3 | アプリケーション状態とイベントループを定義する | `type:feature`, `area:tui`, `priority:p1` | #1, #2 |
| 4 | Linux 向け GitHub Actions CI を構築する | `type:feature`, `area:infra`, `priority:p1` | #1 |
| 5 | 起動対象パスを解決する | `type:feature`, `area:workspace`, `priority:p1` | #1 |
| 6 | Markdown ファイルを再帰探索する | `type:feature`, `area:workspace`, `priority:p1` | #5 |
| 7 | `.gitignore` を探索へ適用する | `type:feature`, `area:workspace`, `priority:p1` | #6 |
| 8 | ファイルツリーモデルを定義する | `type:feature`, `area:workspace`, `priority:p1` | #6 |
| 9 | ファイル変更監視を実装する | `type:feature`, `area:workspace`, `priority:p1` | #3, #7, #8 |
| 10 | 2 ペインレイアウトを実装する | `type:feature`, `area:tui`, `priority:p1` | #2, #3 |
| 11 | ファイルツリー Widget を実装する | `type:feature`, `area:tui`, `priority:p1` | #8, #10 |
| 12 | ペイン間のフォーカス切替を実装する | `type:feature`, `area:tui`, `priority:p1` | #3, #10, #11 |
| 13 | Preview スクロールを実装する | `type:feature`, `area:tui`, `priority:p1` | #3, #10, #16 |
| 14 | Terminal resize を処理する | `type:feature`, `area:terminal`, `priority:p1` | #2, #3, #10 |
| 15 | Markdown parser と内部 Document Model を選定する | `type:research`, `area:markdown`, `priority:p1` | #1 |
| 16 | 見出しと段落を描画する | `type:feature`, `area:markdown`, `priority:p1` | #3, #10, #15 |
| 17 | リスト、Task List、Blockquote を描画する | `type:feature`, `area:markdown`, `priority:p1` | #16 |
| 18 | Inline Code と Code Block を描画する | `type:feature`, `area:markdown`, `priority:p1` | #16 |
| 19 | GFM Table を描画する | `type:feature`, `area:markdown`, `priority:p1` | #16 |
| 20 | 日本語・Unicode 幅と折り返しを処理する | `type:feature`, `area:markdown`, `priority:p1` | #16 |
| 21 | Link と Image placeholder を描画する | `type:feature`, `area:markdown`, `priority:p1` | #16 |

## 推奨する実施順

最初の縦切りは #1 → #2 → #3 → #5 → #6 → #8 → #10 → #11 → #15 → #16 → #13 → #12 → #14 → #7 → #9 の順で到達する。#4 は #1 の直後に並行して実施できる。#17〜#21 は縦切り後に GFM 表現を段階的に完成させる。

## 最初に着手する Issue

**#1: Rust プロジェクトと基本 CLI を初期化する** を選定する。リポジトリに `Cargo.toml`、ソース、CI がまだ存在せず、以後のすべての実装・検証の前提となるためである。
